export type Params = { params: { id: string } };

export type Article = { 
    id: string;
    title: string; 
    date: string;
 };

export type Product = {
    id: string;
    name: string;
    price: number;
    description: string;
    imageUrl: string;
};